openapi: 3.0.0
info:
  title: Sample API
  version: 1.1.0

paths:
  /users:
    post:
      responses:
        "201":
          description: User created
  /customers:
    get:
      responses:
        "200":
          description: Customer list

components:
  schemas:
    User:
      type: object
      required:
        - id
      properties:
        id:
          type: string